# Phase 0 Pilot Decision

> **Final decision: `{{ decision }}`**
> Generated by `scripts/pilot_decision.py` at `{{ generated_at }}` (git `{{ git_sha }}`).
> Reference: plan §5 (pilot), §5.7 (green-light criteria), §5.8 (decision tree), §13.1.

---

## 1. Executive summary

| Criterion | Pass | Value | Threshold | Notes |
|---|---|---|---|---|
{% for c in phenomenon -%}
| {{ c.name }} | {{ "✅" if c.passed else "❌" }} | {{ "%.3f"|format(c.value) }} | {{ "%.3f"|format(c.threshold) }} | {{ c.detail }} |
{% endfor -%}
{% for c in (predictor or []) -%}
| {{ c.name }} | {{ "✅" if c.passed else "❌" }} | {{ "%.3f"|format(c.value) }} | {{ "%.3f"|format(c.threshold) }} | {{ c.detail }} |
{% endfor %}

Phenomenon block: **{{ "PASS" if phenomenon_pass else "FAIL" }}**
Predictor block:  **{{ "PASS" if predictor_pass else ("UNTESTABLE" if predictor is none else "FAIL") }}**

---

## 2. Phenomenon results

- Pairs with stable sign heterogeneity (criterion 1): **{{ phenomenon_inputs.n_pairs_with_sign_het }}** of {{ phenomenon_inputs.n_meaningful_pairs }} meaningful pairs.
- Heterogeneity prevalence among meaningful pairs (criterion 2): **{{ "%.1f%%"|format(100 * phenomenon_inputs.pct_meaningful_with_sign_het) }}**.
- Random-partition null (criterion 3): observed prevalence **{{ "%.3f"|format(phenomenon_inputs.observed_prevalence) }}** vs 95th percentile **{{ "%.3f"|format(phenomenon_inputs.null_percentile_threshold) }}** (empirical p = {{ "%.3g"|format(phenomenon_inputs.null_p_value) }}).

Per-dataset prevalence:

| Dataset | Pairs | Meaningful | Sign-heterogeneous | Prevalence |
|---|---|---|---|---|
{% for row in per_dataset -%}
| {{ row.dataset }} | {{ row.n_pairs }} | {{ row.n_meaningful }} | {{ row.n_sign_het }} | {{ "%.1f%%"|format(100 * row.prevalence) }} |
{% endfor %}

See `outputs/figures/pilot_random_null_violin.pdf` for the random-null violin plot.

---

## 3. Predictor results

{% if predictor is none -%}
**Predictor block was untestable.** No meaningful pairs survived the §2.12
filter (or none had ≥3 valid regions), so AUROC / Spearman / cross-seed
agreement are undefined. The decision tree treats this as an
"untestable predictor" path (plan §5.8).
{%- else -%}
| Predictor | AUROC vs sign(Δ) |
|---|---|
| `G_ij(r)` | **{{ "%.3f"|format(predictor_inputs.auroc_g_ij) }}** |
{% for name, value in baselines_by_name.items() -%}
| {{ name }} | {{ "%.3f"|format(value) }} |
{% endfor -%}

- Best non-gradient baseline: **{{ predictor_inputs.best_baseline_name }}** (AUROC {{ "%.3f"|format(predictor_inputs.auroc_best_baseline) }})
- Spearman ρ between `G_ij(r)` and `Δ_ij(r)`: **{{ "%.3f"|format(predictor_inputs.spearman_rho) }}**
- Cross-seed sign agreement: **{{ "%.1f%%"|format(100 * predictor_inputs.cross_seed_agreement) }}**
{%- endif %}

---

## 4. Three example task pairs

{% for ex in examples -%}
### {{ ex.dataset }}: ({{ ex.task_i }}, {{ ex.task_j }}) — {{ ex.label }}

- Δ_ij(r) range across regions: [{{ "%.3f"|format(ex.delta_min) }}, {{ "%.3f"|format(ex.delta_max) }}]
- Sign-heterogeneous: **{{ "yes" if ex.sign_het else "no" }}**
- C_ij = {{ "%.2f"|format(ex.c_ij) }}, H_ij = {{ "%.2f"|format(ex.h_ij) }}

{% endfor %}
See `outputs/figures/pilot_figure_2_preview.pdf` for regional benefit bar charts of these three pairs.

---

## 5. Decision tree (plan §5.8)

The walked-through reasoning that produced the decision above:

{% for line in decision_trace -%}
{{ loop.index }}. {{ line }}
{% endfor %}

---

## 6. Final decision

**`{{ decision }}`**

{{ decision_explanation }}

> Wave 4+ proceeds **only after a human reviewer (PI sign-off) signs this document.** A7 does not unilaterally advance the project past this gate.
